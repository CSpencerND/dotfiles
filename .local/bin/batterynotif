#!/usr/bin/env bash

while true; do
	ac=$(cat /sys/class/power_supply/AC/online)
	if [ "$ac" -eq 1 ]; then
		continue
	fi
	bat=$(cat /sys/class/power_supply/BAT0/capacity)
	notify-send "🔋 Battery $bat%"
	if [ "$bat" -le 42 ]; then
		notify-send -u critical -t 5000 "🔋 Plug in the charger!"
		mpv ~/Music/warning-notification.wav
	fi
	sleep 5m
done
